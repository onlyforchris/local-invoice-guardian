# 2026-09-09

## v1.1.8 发布：费用说明链接更换
- 「查看费用说明」链接由 `https://docs.bigmodel.cn/cn/faq/fee-issues` 改为 `https://docs.bigmodel.cn/cn/guide/start/pricing#%E8%A7%86%E8%A7%89%E7%90%86%E8%A7%A3`（智谱定价页视觉理解区块，与 GLM-4V-Flash 计费对齐），仅 index.html 一处。
- CHANGELOG v1.1.8 条目补记该变更；随工作区内全部 v1.1.8 改动（三步工作流重构、Key 主动检测等，共 4 文件 +159/-84）一并提交：commit bcb61aa "Release v1.1.8"。test_core.py 通过（core checks: ok）。

## 结果列表改版：去分栏 + 弹框原文件预览
- 老板反馈：列表字段挤压、右侧详情面板要点击才能看且名不符实（无预览）→ 拍板去掉分栏、改弹框带真实预览。
- index.html：删除 .results-layout/aside#detail，表格占满全宽；金额/日期/状态/操作列 nowrap，销售方超长省略号+title 悬停；新增 #detailDialog（左字段核对+右 PDF iframe/图片 img 预览，底部标记已使用按钮联动 setUsed 刷新弹框标签）。
- app.py：新增 GET /api/file?path=，只读发票原文件；安全边界=扩展名∈INVOICE_EXTS + is_within(已配置 watch/used dirs)，防穿越已验证（win.ini 与 ..\ 穿越 → 404）；大写 .PDF 扩展名 200。
- 冒烟：143874B jpg 字节级一致、pdf/jpeg content-type 正确；test_core.py 过；node --check 内联 JS 过。commit 062774c。
