# 更新记录

## v1.4.3 (2026-09-10)

修复两张用户实测票暴露的识别问题，并解决「一次性识别故障被缓存固化」。

- **金额抓错（严重）**：`打车377.pdf` 金额显示 ¥11.00，实际价税合计 **¥377.67**
  - 根因：滴滴/高德类电子发票是「数字在前、¥ 在后」版式（`合 计 366.67¥ 11.00¥`、`（ 小 写 ） 377.67¥叁佰…`）。
    稀疏数字归一化把两笔金额拼成 `366.67¥11.00¥`，凭空造出一个假 `¥11.00`；
    而三个金额正则全部因「标签被逐字拆开」（合 计 / 价 税 合 计 / 小 写）和 ¥ 后置而失配，落到 ¥ 兜底抓了假值
  - 修复：① 「价税合计 / 合计 / 小写」标签内空格先归位再匹配；② 新增「价税合计后首个数字」模式（兼容 ¥ 后置）；
    ③ ¥ 兜底同时收「¥在数字前」和「¥在数字后」两种写法再取最大
  - 该票自检机制正确拦下了这次错值（366.67 + 11.00 ≠ 11.00），防错流入报销的设计有效
- **销售方漏识别**：`1.56元.pdf` 销售方为空
  - 根因：该 PDF 中文字体 ToUnicode 映射损坏（文本层中文全是乱码），扫描那一刻本机识别引擎恰不可用，
    走了 AI 兜底，而 AI 没读出销售方；**且这个一次性失败被台账缓存永久固化**，后续扫描不再重试
  - 修复：① 台账记录识别来源（`ocr_engine`: local/ai）；② **缓存自愈**——上次走 AI 仍缺关键字段、
    而本机识别现已可用的票，下轮扫描自动用本机识别重试一次（免费离线；重试后无论成败都不再反复空转）
- **如实标注识别来源**：列表/详情原来把本机识别也标成「AI 识别」，现区分「本机识别 / AI 识别 / 本地解析」
- 台账引擎版本 9 → 10（本次扫描全量重解析一次，之后恢复增量）
- 回归：`打车377.pdf` 金额 37767 分、自检通过；`1.56元.pdf` 销售方「杭州杭千高速公路发展有限公司」；
  全库 119 张 warn=0、金额自检 119/119 通过；新增 ¥ 后置版式单元测试

## v1.4.2 (2026-09-10)

修复两张新版式数电票的识别问题（用户实测）。

- **销售方识别失败**（例：上海市长宁区杨震小吃店）
  - 公司名结尾枚举过窄：只有"有限公司/个体工商户/工作室"等，以"小吃店/烧饼店/商行/超市/餐厅"结尾的个体小商户全部漏识别 → 扩展结尾词表并把"店"纳入
- **购买方抓成杂串**（例：`,16.4020260908,,b017上海亿流科技有限公司`）
  - 根因：数电票"机器可读区"杂流（号码,金额,日期,b017）经稀疏归一化后紧贴公司名，公司名正则允许字母开头导致杂串粘连；税号间隙取文本时又把杂流整段吞成候选
  - 修复：公司名必须以汉字/括号开头；税号间隙内改用公司名正则提取而非整段收入
- 台账引擎版本 9（升级后首次扫描全量重解析，一次性；之后仍是增量）
- 已用两张真实票回归验证，新增该版式的单元测试断言

### v1.4.2 补记

- **修正显示版本号**：上轮只升了引擎版本、漏改界面显示版本，导致页面仍显示 v1.4.1，误以为修复没生效
- **新增「代码已更新」自检**：服务启动时记录 `app.py` 修改时间，之后每次请求比对。
  代码被更新但服务没重启时，页面直接红色提示「重新双击启动发票管家.bat」，不再让用户对着旧版本排查
- **修正原版本提示的方向**：旧逻辑比较"页面版本 vs 服务版本"，但两者都由同一进程输出，永远相等，等于失效。
  现改为两种情况分别提示：代码过期 → 重启程序；仅页面旧 → Ctrl+F5 刷新

## v1.4.1 (2026-09-10)

**修复：升级后已识别过的票仍显示"未识别"**（用户实测：发票文件21.pdf 重启后依旧失败）。

- 根因：上一版改了解析逻辑但**没有升级台账引擎版本**，查重时按缓存直接复用了旧台账里的错误识别结果，永远不会重新解析
- 修复：台账引擎版本 7 → 8，升级后自动全量重新解析一次
- 注意：升级后**第一次扫描会明显变慢**（119 张票全部重新解析，图片票走本机识别约需半分钟到一分钟），属一次性成本，之后恢复增量速度
- 同时：本机识别组件改为全自动安装（首次启动后台静默装，界面只显示友好进度，不再出现任何技术提示）

## v1.4.0 (2026-09-10)

识别环节不再依赖任何外部产品：本机 OCR 兜底 + OFD 数电票支持。

- **本机 OCR 识别（RapidOCR）**：图片发票与扫描件现在优先在本机识别，**不需要 API Key、不联网、票据不出本机**。实测一张餐饮发票端到端约 4.6 秒，号码/日期/金额/购销方全部正确
- 识别链路调整为：**PDF/OFD 文本层 → 本机 OCR → AI 识别**。本机组件可用时优先本机；本机不可用或识别为空时才走智谱，Key 从「必经路径」降级为「可选增强」
- **OFD（数电票官方格式）支持**：直接解析 OFD 的 zip+XML 票面文本，零第三方依赖、不联网。样本验证号码/日期/金额/票种/购销方全部正确；OFD 文件在详情中提示用官方阅读器看图（页面暂不支持预览该格式）
- 界面新增本机识别状态提示；未安装时给出「安装本地识别组件.bat」指引
- 新增 `安装本地识别组件.bat`：一键安装本地识别（清华源优先，失败回退官方源，失败不影响使用）
- `安装并启动.bat` 增加可选步骤：首次安装时尝试装本地识别，**装不上也不阻断**，主依赖仍为阻断式校验
- 依赖拆分为 `requirements.txt`（核心，必需）与 `requirements-ocr.txt`（本地识别，可选）
- 已装组件的机器：重启「启动发票管家.bat」后界面显示「本机 OCR 已就绪」即生效

## v1.3.1 (2026-09-10)

修复新版数电票 PDF 整类漏识别（用户实测：号码/金额/日期全部未识别，仅销售方识别成功）。

- **根因**：新版数电票文本层「标签与值分离 + 数值逐位拆空格」——“发票号码：”后直接换行，值出现在文末且形如 `2 6 3 3 …`、`¥5 8 . 5 8`、`2 0 2 6 年0 9 月0 9 日`，原有正则全部失配
- 新增稀疏数字归一化：压缩数字/¥/小数点之间的空白，号码（20 位兜底）、开票日期、价税合计三字段一并恢复识别
- 公司名修复：“金匠匠（杭 州）餐饮管理有限公司”这类名称内夹空格不再被截断为“州)餐饮管理有限公司”；被截断的残缺候选自动让位于完整名称
- 票种判定：`电子发票（普通/专用发票）` 正确归为「数电票」，与老版「增值税普通/专用发票」区分
- 结案交互：原生 prompt/confirm 弹窗改为页面内对话框（批次预填当月、归档选项勾选），结案完成后自动重新查重
- 已用用户真实发票（发票文件21.pdf）验证：号码/日期/金额/购买方/销售方/票种全部正确，校验与抬头检查通过

## v1.3.0 (2026-09-10)

面向财务报销审核环节的闭环补强（核心是"用过一次，下月自动认得它"）。

- **已报销永久库**（`used_ledger.json`）：发票记录脱离文件位置存在，原文件被移动或删除后仍持续参与查重；结案或导入历史台账即写入，重复自动跳过
- **一键结案**：勾选本批发票后一次完成「标记已报销 → 写入永久库 → 按 `日期_销售方_金额_号码` 规范命名复制归档」；既缺号码又缺金额的票会明确列出，不会悄悄漏进库
- **历史台账导入（冷启动）**：财务已有的 Excel 台账另存为 CSV 后粘贴导入，自动识别号码/金额/日期/销售方列（列顺序不限），导入后查重立即生效，解决"第一个月查不出重复"
- **台账备份与损坏保护**：每次写入前滚动备份（保留 5 份）；台账损坏时不再静默当空台账处理，改为保留现场并提示从备份还原；还原时不再覆盖同名备份
- **字段自校验**：发票号码位数（8 位 / 数电 20 位）、开票日期合理性、金额异常、以及「不含税合计 + 税额 = 价税合计」自洽校验，抓不到就不校验以免误报
- **抬头校验**：在「其他设置」填写本公司名称后，自动标记"抬头与本公司不符"，拦截个人票混报
- **查验状态与退回**：每张票可标记「未查验 / 已查验通过 / 查验异常」与退回原因（重复报销/抬头不符/疑似假票/金额不符/票面信息不全/其他），支持批量设置
- **审核底稿导出**：一键导出 xlsx，含明细、按提交人（目录）小计、合计与独立「退回清单」页，可直接打印存档
- **查重可解释**：详情弹窗列出每条重复命中的级别、依据、来源文件/批次与是否已报销
- 顶部常驻显示「已报销库 N 张 · 台账 M 条」

> 说明：本工具不提供发票真伪鉴定，真伪以国家税务总局查验结果为准；查验状态仅用于记录与留痕。

## v1.2.1 (2026-09-09)

- 所有通知改为右上角弹框（toast）：不再占用底部面板空间；普通提示 5 秒、错误提示 10 秒自动消失，也可点 ✕ 立即关闭；多条通知自动堆叠
- 「正在扫描」进行中提示同样为弹框，扫描结束后自动收起，再出结果通知
- 修正配置弹窗与使用说明中的智谱平台链接为 https://open.bigmodel.cn/apikey/platform

## v1.2.0 (2026-09-09)

- 目录选择全面改为「条目（chip）」形式：选中后显示为带 × 删除按钮的条目，不再用手输输入框，杜绝「选择器返回 C:/ 正斜杠、手输 C:\ 反斜杠」显示不一致的困惑；统一显示为 Windows 反斜杠路径
- 只有「历史已使用发票目录」支持多个（旁边「添加目录」逐个追加）；「本次新发票目录」和「复制归档目录」均为单个，重新选择即替换
- 顶部提示条不再常驻：普通提示 5 秒后自动消失；扫描进行中的提示保持到扫描结束；错误提示保留到下一次操作
- 未选新票目录就点「开始查重」时，目录框红框高亮并滚动定位提示
- 悬停条目可见完整路径；删除后立即从本次查重范围移除

## v1.1.9 (2026-09-09)

- 智谱 API Key 改为「网页内直接粘贴」：弹窗给出大白话三步指引（打开智谱官网注册 → 创建并复制 Key → 粘贴后点「保存并检测」），保存后自动做一次连通性检测，彻底去掉 PowerShell 命令行步骤
- 新增 /api/save-key 接口：Key 写入 Windows 用户环境变量（与旧命令行方式同位置、互相兼容），当前进程立即生效，无需重启程序
- 弹窗显示本机已保存 Key 的脱敏状态（仅头尾各 4 位）；提示"Key 无效"时直接在弹窗里重新粘贴即可
- 使用说明同步改写为粘贴式配置流程；旧版命令行配置的 Key 继续有效

## v1.1.8 (2026-09-09)

- 重构为“选择发票—选择识别—查看结果”三步工作流，取消核心配置的双层折叠
- 新增 Windows 文件夹选择器、异常优先筛选和分页
- 结果列表取消右侧详情分栏：表格占满全宽，金额/日期/状态不再挤压换行，超长销售方省略号截断（悬停可见全名）
- 「查看」改为弹框：左侧核对提取字段并可直接标记已使用，右侧内嵌预览发票原文件（PDF/图片；新增 /api/file 只读接口，仅允许已配置目录内的发票文件）
- 「添加目录」支持一次加入多个目录：选择父目录后勾选其子目录（年月日/发票/报销/归档命名的自动勾选），确认后批量加入
- 查重结果区默认展开：未扫描时显示空态提示，界面不再空荡
- 预览加载失败或检测到「页面已升级、服务仍是旧进程」时，明确提示重启「启动发票管家.bat」（HTML 即时生效但接口随进程，升级后必须重启）
- AI 识别改为本地/AI 双选卡，常驻展示隐私、免费政策和可用性边界
- 新增智谱 Key 主动检测，并在 Key 无效、限流、超时或服务异常时给出恢复操作
- 「查看费用说明」链接改为智谱官方定价页并直接定位到视觉理解模型区块，与所用 GLM-4V-Flash 计费信息对齐

## v1.1.7 (2026-09-09)

- 补齐快递、网约车、燃油、技术服务和员工食品等默认分类关键词
- CSV/xlsx 的“本次新票”导出范围与页面页签保持一致
- 在线升级改为跟随 GitHub 最新正式 Release，并自动补装新增依赖
- 启动脚本仅关闭确认属于发票管家的旧服务，不再误杀其他 8765 进程
- README 与使用说明补充 xlsx 汇总和在线升级入口

## v1.1.6 (2026-09-08)

- 新增「升级发票管家.bat」+ update.py：双击即可从 GitHub 拉取最新版本自动覆盖升级并重启；配置、台账等用户数据绝不覆盖；失败时提示代理设置步骤
- 启动/安装/升级脚本启动前自动结束占用 8765 端口的旧服务进程，不再需要命令行手动杀进程
- 扫描解析并发化（4 线程）：批量票据首次启用 AI 识别时整轮提速约 4 倍
- 扫描等待提示改为明确的"正在扫描识别…约需 1 分钟"，避免误以为卡死
- 修复：配置智谱 API Key 后仍提示"未配置"——Windows 设置用户级环境变量后，已在运行的程序不会刷新环境（双击启动的进程继承旧快照）。现在实时读取注册表中的用户级/系统级变量，配置后无需重启电脑，重新扫描即可生效
- 新增「导出汇总表」：一键导出 xlsx 分类汇总——分类做列、金额按开票日期竖排、底部合计行（SUM 公式）与总计，另附明细页便于财务对账
- 依赖新增 openpyxl；安装脚本同步检查并自动补装，已装环境不受影响
- 明细与汇总中无开票日期的记录排序沉底，不影响有日期票据的时间顺序
- 安装脚本重写（v2）：检测 Python 时逐个真实运行版本自检命令，自动跳过微软商店占位程序
- .venv 创建失败或残留不完整时自动清理，降级为 `pip install --user` 模式（免管理员）
- pip 双源均失败时给出具体代理配置步骤；全流程按 [1/4]-[4/4] 分步报告，失败可定位步骤
- 安装成功后写入 python_path.txt，启动脚本自动复用检测到的 Python（--user 模式下也能双击启动）
- 安装脚本升级：依赖安装优先清华镜像、失败自动回退官方源
- 未安装 Python 时尝试用 winget 静默自动安装，免管理员权限
- 已装过依赖时跳过安装步骤，双击即秒级启动
- 换行符统一为 CRLF，规避 cmd 对 LF 批处理文件的兼容风险

## v1.1.5

- 修复 AI OCR 在“小写”金额后返回中文冒号时金额未提取
- “需人工复核”摘要只统计本次新票，不再混入历史库

## v1.1.4

- 将识别方式明确为“使用 AI 识别”和“本地 Python 识别”，并说明差异
- 页面显示智谱 API Key 配置状态、申请入口和配置命令
- 扫描完成后明确提示仍有多少张票据字段未提取

## v1.1.3

- 识别不带“有限公司”后缀的餐饮店等销售方名称
- 修复 PDF 兼容汉字导致企业名称被截断
- 将“餐费”补入人事福利默认分类规则

## v1.1.2

- 将零食、食品默认归入人事福利，移除业务招待费中的重复二级词
- 修复部分通行费 PDF 文本层夹杂 NUL 导致号码、金额和日期无法识别
- 文本层损坏时自动改走 AI OCR，并明确标记缺失字段
- 说明本次新票、历史已用票库、人工状态和查重结论之间的关系

## v1.1.1

- 清理示例中的本机目录与公司信息
- 增加一键安装入口、开源许可和智谱 API Key 配置说明
- 同时支持 `ZHIPUAI_API_KEY` 与智谱文档常用的 `ZAI_API_KEY`

## v1.1.0

- 内容级发票查重与已使用票识别
- 图片及扫描 PDF 的 AI OCR
- 财务两级分类、分类维护与单票人工改类
- CSV 导出、状态维护与复制归档
