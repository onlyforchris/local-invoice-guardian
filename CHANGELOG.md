# 更新记录

## v1.2.0 (2026-09-09)

- 目录选择全面改为「条目（chip）」形式：选中后显示为带 × 删除按钮的条目，不再用手输输入框，杜绝「选择器返回 C:/ 正斜杠、手输 C:\ 反斜杠」显示不一致的困惑；统一显示为 Windows 反斜杠路径
- 只有「历史已使用发票目录」支持多个（旁边「添加目录」逐个追加）；「本次新发票目录」和「复制归档目录」均为单个，重新选择即替换
- 顶部提示条不再常驻：普通提示 5 秒后自动消失；扫描进行中的提示保持到扫描结束；错误提示保留到下一次操作
- 未选新票目录就点「开始查重」时，目录框红框高亮并滚动定位提示
- 悬停条目可见完整路径；删除后立即从本次查重范围移除

## v1.1.9 (2026-09-09)

- 智谱 API Key 改为「网页内直接粘贴」：弹窗给出大白话三步指引（打开智谱官网注册 → 创建并复制 Key → 粘贴后点「保存并检测」），保存后自动做一次连通性检测，彻底去掉 PowerShell 命令行步骤
- 新增 /api/save-key 接口：Key 写入 Windows 用户环境变量（与旧命令行方式同位置、互相兼容），当前进程立即生效，无需重启程序
- 弹窗显示本机已保存 Key 的脱敏状态（仅头尾各 4 位）；提示"Key 无效"时直接在弹窗里重新粘贴即可
- 使用说明同步改写为粘贴式配置流程；旧版命令行配置的 Key 继续有效

## v1.1.8 (2026-09-09)

- 重构为“选择发票—选择识别—查看结果”三步工作流，取消核心配置的双层折叠
- 新增 Windows 文件夹选择器、异常优先筛选和分页
- 结果列表取消右侧详情分栏：表格占满全宽，金额/日期/状态不再挤压换行，超长销售方省略号截断（悬停可见全名）
- 「查看」改为弹框：左侧核对提取字段并可直接标记已使用，右侧内嵌预览发票原文件（PDF/图片；新增 /api/file 只读接口，仅允许已配置目录内的发票文件）
- 「添加目录」支持一次加入多个目录：选择父目录后勾选其子目录（年月日/发票/报销/归档命名的自动勾选），确认后批量加入
- 查重结果区默认展开：未扫描时显示空态提示，界面不再空荡
- 预览加载失败或检测到「页面已升级、服务仍是旧进程」时，明确提示重启「启动发票管家.bat」（HTML 即时生效但接口随进程，升级后必须重启）
- AI 识别改为本地/AI 双选卡，常驻展示隐私、免费政策和可用性边界
- 新增智谱 Key 主动检测，并在 Key 无效、限流、超时或服务异常时给出恢复操作
- 「查看费用说明」链接改为智谱官方定价页并直接定位到视觉理解模型区块，与所用 GLM-4V-Flash 计费信息对齐

## v1.1.7 (2026-09-09)

- 补齐快递、网约车、燃油、技术服务和员工食品等默认分类关键词
- CSV/xlsx 的“本次新票”导出范围与页面页签保持一致
- 在线升级改为跟随 GitHub 最新正式 Release，并自动补装新增依赖
- 启动脚本仅关闭确认属于发票管家的旧服务，不再误杀其他 8765 进程
- README 与使用说明补充 xlsx 汇总和在线升级入口

## v1.1.6 (2026-09-08)

- 新增「升级发票管家.bat」+ update.py：双击即可从 GitHub 拉取最新版本自动覆盖升级并重启；配置、台账等用户数据绝不覆盖；失败时提示代理设置步骤
- 启动/安装/升级脚本启动前自动结束占用 8765 端口的旧服务进程，不再需要命令行手动杀进程
- 扫描解析并发化（4 线程）：批量票据首次启用 AI 识别时整轮提速约 4 倍
- 扫描等待提示改为明确的"正在扫描识别…约需 1 分钟"，避免误以为卡死
- 修复：配置智谱 API Key 后仍提示"未配置"——Windows 设置用户级环境变量后，已在运行的程序不会刷新环境（双击启动的进程继承旧快照）。现在实时读取注册表中的用户级/系统级变量，配置后无需重启电脑，重新扫描即可生效
- 新增「导出汇总表」：一键导出 xlsx 分类汇总——分类做列、金额按开票日期竖排、底部合计行（SUM 公式）与总计，另附明细页便于财务对账
- 依赖新增 openpyxl；安装脚本同步检查并自动补装，已装环境不受影响
- 明细与汇总中无开票日期的记录排序沉底，不影响有日期票据的时间顺序
- 安装脚本重写（v2）：检测 Python 时逐个真实运行版本自检命令，自动跳过微软商店占位程序
- .venv 创建失败或残留不完整时自动清理，降级为 `pip install --user` 模式（免管理员）
- pip 双源均失败时给出具体代理配置步骤；全流程按 [1/4]-[4/4] 分步报告，失败可定位步骤
- 安装成功后写入 python_path.txt，启动脚本自动复用检测到的 Python（--user 模式下也能双击启动）
- 安装脚本升级：依赖安装优先清华镜像、失败自动回退官方源
- 未安装 Python 时尝试用 winget 静默自动安装，免管理员权限
- 已装过依赖时跳过安装步骤，双击即秒级启动
- 换行符统一为 CRLF，规避 cmd 对 LF 批处理文件的兼容风险

## v1.1.5

- 修复 AI OCR 在“小写”金额后返回中文冒号时金额未提取
- “需人工复核”摘要只统计本次新票，不再混入历史库

## v1.1.4

- 将识别方式明确为“使用 AI 识别”和“本地 Python 识别”，并说明差异
- 页面显示智谱 API Key 配置状态、申请入口和配置命令
- 扫描完成后明确提示仍有多少张票据字段未提取

## v1.1.3

- 识别不带“有限公司”后缀的餐饮店等销售方名称
- 修复 PDF 兼容汉字导致企业名称被截断
- 将“餐费”补入人事福利默认分类规则

## v1.1.2

- 将零食、食品默认归入人事福利，移除业务招待费中的重复二级词
- 修复部分通行费 PDF 文本层夹杂 NUL 导致号码、金额和日期无法识别
- 文本层损坏时自动改走 AI OCR，并明确标记缺失字段
- 说明本次新票、历史已用票库、人工状态和查重结论之间的关系

## v1.1.1

- 清理示例中的本机目录与公司信息
- 增加一键安装入口、开源许可和智谱 API Key 配置说明
- 同时支持 `ZHIPUAI_API_KEY` 与智谱文档常用的 `ZAI_API_KEY`

## v1.1.0

- 内容级发票查重与已使用票识别
- 图片及扫描 PDF 的 AI OCR
- 财务两级分类、分类维护与单票人工改类
- CSV 导出、状态维护与复制归档
